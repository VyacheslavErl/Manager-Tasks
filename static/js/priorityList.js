/* const priority = document.getElementById('priority');

priority?.addEventListener('change', function() {
  const colors = ["#FFFFFF", "#7091C2", "#70C273", "#C2A070", "#C27470"];
  if (priority && priority.selectedIndex >= 0 && priority.selectedIndex < colors.length) {
    priority.style.backgroundColor = colors[priority.selectedIndex];
  }
});
 */